sudo: required

services:
  - docker

before_script:
  - docker build --tag matchmaker .
  - docker run --name db --publish 5433:5432 --detach siggame/colisee-db

script:
  - docker run -e POSTGRES_PORT=5433 matchmaker --network host npm run test

after_script:
  - docker stop db